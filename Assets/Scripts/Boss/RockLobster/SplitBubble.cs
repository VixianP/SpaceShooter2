using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SplitBubble : MonoBehaviour
{

    float _size;

    Vector3 _CurrentSize;

    float _Damage;

    public 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

  

    void MergeMovement()
    {
        //opposing movement to go into each other
        //simply check if its greater or less than zero
    }
}
